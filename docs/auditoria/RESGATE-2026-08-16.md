# Operação Resgate — 16/08/2026 (relatório parcial)

> Sessão autônoma (Bruce). Branch de trabalho: `fix/resgate-16-08` (NÃO deployado —
> gate da Fase 8 pendente). Produção no momento da auditoria: build `gpGBKaEz…`,
> deployado às 18:44 de 16/08 (master `05b0424`).

## FASE 0 — Reconhecimento

| Item | Estado |
|---|---|
| Repo | `Desktop\💻 Dev\portal-miozorio` @ master, limpo, sincronizado. (`portal-miozorio-crm2` = checkout paralelo do MESMO remote, branch crm2 — não usar para hotfix.) |
| Rotas | 56 páginas (site público + /admin + /clube) · 28 route handlers |
| Prisma | schema válido; **sem campos `Decimal`** (dinheiro em cents) |
| Banco dev local | Postgres em localhost:5432 existe, credencial do `.env` inválida (P1000) — sem reprodução local por ora |
| SSH VPS | **indisponível** (publickey/password negados; sem chave em ~/.ssh). Caminho de infra = painel Dokploy `http://76.13.230.78:3000` |
| Produção | `/api/health`: app ok, db ok. Health detalhado (crons) exige `?token=CRON_SECRET` |
| Evolution | Dedicada da Mi: `evo.miozorio.com.br` — **v2.3.7** (última estável; 2.4.0-rc exige licença). Instância `evo_miozorio`, `whatsappWebVersion 2.3000.1045318025` |

## BUG A — /admin/clientes/[id] ("Ops, algo deu errado")

**Estado: NÃO REPRODUZÍVEL no código atual; digest pendente.**

- Análise estática completa da página e de TODAS as dependências de render
  (`getSaldoExtrato`, `contarIndicacoesFechadas`, `lerAnamnese`, `getSettings`,
  `formatBRL/formatPhoneBR/waLink`, `STATUS_LABEL/STYLE`, relações do include):
  nenhuma via de crash. Guardas `notFound()`, `.catch(() => null)`, mapas totais,
  defaults em settings.
- A página foi retrabalhada em 15/08 e produção recebeu 3 deploys em 15–16/08.
  Hipótese principal: **o print é anterior aos deploys recentes** (ou o gatilho é
  um dado específico que só o digest vai apontar).
- H1 (Decimal) descartada — não há Decimal no schema. H2/H3/H5/H7 já defendidas
  no código. H6 (migrate status em prod) pendente de verificação no Dokploy.
- **Correção preventiva aplicada** (`6bbee22`): `src/app/admin/error.tsx` agora
  mostra `Código do erro: {digest}` — o próximo print da Mi já chega diagnosticável.

**Para fechar:** logar no admin de produção, abrir o cliente
`8af7613b-a52b-4811-9563-837172ffb75b` e (a) se abrir = resolvido pelos deploys
de 15–16/08; (b) se quebrar = ler o digest na tela (após deploy desta branch) e
correlacionar no log do container via Dokploy.

## BUG B — Evolution / WhatsApp ("Não é possível conectar novos dispositivos")

**Matriz de isolamento (versão reduzida):**

| Teste | Resultado |
|---|---|
| A. Versão da imagem | ✅ **v2.3.7 = última estável** → upgrade de major DESCARTADO (2.4.0-rc traz licença obrigatória — não subir) |
| B. Relógio da VPS | ⏳ pendente — precisa do painel Dokploy (sem SSH) |
| C. Logs do Baileys | ⏳ pendente — idem |

- Sessão anterior de HOJE já diagnosticou e corrigiu: instância com registro
  corrompido no banco da Evolution (pareamento sem deixar rastro em log verboso),
  QR rotacionando a cada consulta (cache 18s), logout sem guarda derrubando o
  container (428 Baileys). Tudo em produção desde 18:44 (PRs #92–#96).
- Portal já cumpre a Fase 4.3: payload v2 (`integration: WHATSAPP-BAILEYS`),
  fallback por código de pareamento, polling com feedback, erros visíveis.
- Suspeito restante para a recusa do WhatsApp: `CONFIG_SESSION_PHONE_VERSION`
  fixado em valor velho. O compose do repo fixa `2.3000.1035194821`; a API
  reporta `2.3000.1045318025`. Recomendação da comunidade (issues #1911/#1761):
  **deixar a env VAZIA** — a Evolution busca a versão atual sozinha.

**Para fechar:** no Dokploy, conferir/remover `CONFIG_SESSION_PHONE_VERSION` do
serviço evo-miozorio (+ `timedatectl` no host), reiniciar o container e refazer
o pareamento no celular da Mi via "Conectar com número de telefone" (o portal já
gera o código de 8 dígitos). Validar com `connectionState = open` + mensagem real.

## FASE 5 — Varredura (parcial, sem login)

**S0 — nenhum encontrado:**
- Noiva/debutante: trava no backend confirmada (`booking-service.ts:108` → 422)
  e `/api/services` de produção não expõe os serviços (filtro `bookableOnline`).
  Páginas públicas sem botão de agendar; CTA WhatsApp correto (21) 97022-5231.
- Zero segredo hardcoded (grep limpo). Rotas admin/cron/webhook todas com guarda
  (requireAdmin, CRON_SECRET fail-closed, token de webhook). Middleware cobre /admin.

**Corrigido nesta sessão:**
- S1→ok `next-auth` **crítica** → 4.24.15 (patch, sem breaking) — `1d252db`
- S2→ok `nanoid` alta → 3.3.18 — idem
- S2→ok error boundary do admin sem digest → `6bbee22`

**Backlog S3:**
- Next 14.2.35 tem 2 advisories altas que só fecham no Next 16 (breaking) —
  planejar upgrade fora desta operação.
- 4 serviços de cabelo com `pendingPrice` ("a combinar") agendáveis — decisão de
  negócio pendente da Mi (já listado na auditoria de julho).

**Pendentes (exigem sessão admin ou Dokploy):** crawler autenticado, checklist
por módulo (Agenda/Financeiro/Resumo/…), health detalhado dos crons, RBAC ao vivo.

## QA local

- `vitest`: **236/236 verdes** · `tsc --noEmit`: limpo · `next build`: sucesso
  (com os commits desta branch).

## Bloqueios (precisam do Rodolfo)

1. **Login no painel Dokploy** (aba já aberta no navegador da sessão) — destrava:
   logs do portal (digest do BUG A), env da Evolution (`CONFIG_SESSION_PHONE_VERSION`),
   relógio da VPS, migrate status em prod.
2. **Teste de pareamento no celular da Mi** APÓS o ajuste da env — o erro só se
   manifesta no aparelho.
3. (Opcional) credencial admin de teste p/ crawler autenticado da Fase 5.
